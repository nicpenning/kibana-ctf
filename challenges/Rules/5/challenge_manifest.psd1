@{
    Name = "A Call to Action"
    Category = "Rules"
    RequiredFiles = @(
        "ctfd_challenge.json"
        "ctfd_flag.json"
    )
    Resources = @{
        KibanaVersion = "^9.5.4"
    }
}
